import { ImageMogr2Opts } from '../types';
export declare const imageMogr2: ({ url, type, size, format, interlace }: ImageMogr2Opts) => string;
export declare const imageInfo: (url: string) => string;
